import React from "react";
import ReactDOM from "react-dom/client";
import GamesDesktop from "../../templates/GamesDesktop";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <GamesDesktop />
    </React.StrictMode>
);
